\begingroup
\graphicspath{{../prob_6/Figures/}}


\subsection{Step 1: GAN Training and Initial Generation}

A Generative Adversarial Network (GAN) was trained using 350 real samples per digit. The training process was monitored visually and through loss curves to ensure convergence.

\subsubsection{Training Progress Visualization}
As seen in Figure~\ref{fig:gan_samples}, the GAN consisted of a convolutional discriminator and a transposed convolutional generator. The training process was conducted for several epochs until visually acceptable digit-like samples were obtained.

The generated images were slightly blurred but still recognizable, indicating that the GAN successfully captured the general structure of handwritten digits.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.45\linewidth]{gan_epoch_800.png}
    \includegraphics[width=0.45\linewidth]{gan_epoch_900.png}
    \caption{GAN generated samples during training (epochs 800 and 900)}
    \label{fig:gan_epochs}
\end{figure}

Shown in Figure~\ref{fig:gan_epochs}, from the intermediate outputs, it can be observed that the GAN gradually learns the structure of handwritten digits. Early outputs were noisy, while later epochs produced clearer and more recognizable digits.

\subsubsection{Final Generated Samples}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.6\linewidth]{gan_generated_samples.png}
    \caption{Final samples generated by the trained GAN}
    \label{fig:gan_samples}
\end{figure}

Although the generated digits are slightly blurred, they are still recognizable, indicating that the GAN successfully captured the underlying data distribution.

\subsubsection{Training Stability (Loss Curves)}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.6\linewidth]{gan_loss_curve.png}
    \caption{Generator and Discriminator loss during training}
\end{figure}

The loss curves show the adversarial behavior between the generator and discriminator. After initial instability, both losses stabilize, indicating that the GAN reached a relatively balanced training state.

\subsubsection{Discriminator Analysis}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.6\linewidth]{gan_mistakes.png}
    \caption{Examples where the discriminator misclassified real and fake images}
\end{figure}

Some generated samples were realistic enough to fool the discriminator, while some real images were incorrectly classified. This indicates that the generator learned meaningful patterns, but imperfections still exist.

\subsection{Step 2: Effect of Synthetic Data on Classification Accuracy}

The generated data was combined with different amounts of real data (350, 750, and 1000 samples per digit). The classification accuracy for each configuration is summarized in Table~\ref{tab:gan_results} and visualized in Figure~\ref{fig:gan_results}.

\begin{table}[htbp]
\centering
\caption{Accuracy for different combinations of real and GAN-generated data}
\label{tab:gan_results}
\begin{tabular}{|c|c|c|}
\hline
\textbf{Real per digit} & \textbf{GAN per digit} & \textbf{Accuracy} \\ \hline
350 & 0    & 0.9548 \\ \hline
350 & 1000 & 0.9676 \\ \hline
350 & 1500 & 0.9651 \\ \hline
350 & 2000 & 0.9573 \\ \hline
750 & 0    & 0.9733 \\ \hline
750 & 1000 & 0.9719 \\ \hline
750 & 1500 & 0.9752 \\ \hline
750 & 2000 & 0.9638 \\ \hline
1000 & 0    & 0.9690 \\ \hline
1000 & 1000 & 0.9775 \\ \hline
1000 & 1500 & 0.9735 \\ \hline
1000 & 2000 & 0.9740 \\ \hline
\end{tabular}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\linewidth]{results_plot.png}
    \caption{Accuracy vs. GAN-generated samples per digit}
    \label{fig:gan_results}
\end{figure}

From Table~\ref{tab:gan_results} and Figure~\ref{fig:gan_results}, the following observations can be made:

\begin{itemize}
    \item Adding synthetic data significantly improves performance when the real dataset is small (350 samples).
    \item The best improvement is achieved around 1000 generated samples per digit.
    \item Increasing synthetic data beyond this point does not improve accuracy and may slightly degrade performance.
    \item This behavior is due to the limited quality and diversity of GAN-generated samples.
\end{itemize}

\subsection{Step 3: Reducing the Need for Real Data}

The baseline case using only real data (1000 samples per digit) achieved an accuracy of approximately $96.9\%$.

Using only 350 real samples combined with 1000 GAN-generated samples achieved an accuracy of $96.76\%$, which is very close to the full dataset performance.

This demonstrates that GAN-generated data can significantly reduce the need for real labeled data while maintaining high accuracy.

However, increasing synthetic data beyond an optimal point leads to performance degradation due to limited diversity and artifacts in generated images.

\subsection{Discussion}

The results confirm that GANs can reduce the dependency on real data while maintaining high accuracy.

\begin{itemize}
    \item GAN-generated data is highly effective when real data is limited.
    \item There exists an optimal balance between real and synthetic data.
    \item Excessive synthetic data can reduce performance.
\end{itemize}

\endgroup